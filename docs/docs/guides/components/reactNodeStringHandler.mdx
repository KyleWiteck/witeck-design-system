---
title: ReactNodeStringHandler
description:
  ReactNodeStringHandler is a flexible component for handling text and component
  composition. It automatically wraps string content in a Text component while
  preserving ReactNode structure, with support for pre/post elements like icons
  or badges. Useful for consistent text styling and layout when dealing with
  mixed content types.
---

import Doc from '../../../../lib/src/components/ReactNodeStringHandler/docs.mdx';

<Doc />
