import { PropsTable } from '@site/src/components/PropsTable';

Empty states are designed to prevent confusion when the primary contents of a
page or component cannot be displayed.

Use when:

1. Component or part of UI has no items or data to show;
2. Display for friendly tips;
3. When a user hasn’t interacted with a feature yet;
4. A problem displaying data occurred.

These components provide a structured way to display error messages with a
headline, optional message, image, and configurable action buttons. They are
designed for both basic and advanced use cases, supporting two layout variants:

- `base`: A larger, centered layout for major error states.
- `small`: A compact, inline version for minor errors.

To streamline development, predefined error components handle common scenarios
such as "Not Found" or "Forbidden Access."

## Props

<PropsTable
  hideElementProp
  hideChildren
  defaultElement
  hideSprinkles
  hideAttributes
  hideStyles
  defaultElement="section"
  componentSpecificProps={[
    {
      name: 'variant',
      type: '"base" | "small"',
      default: '"base"',
      description: 'Layout variants, large and compact versions'
    },
    {
      name: 'headline',
      type: 'string',
      default: 'string',
      description: 'Dynamic title based on error provided.'
    },
    {
      name: 'message',
      type: 'ReactNode',
      default: 'string',
      description: 'Dynamic message based on error provided.'
    },
    {
      name: 'containerProps',
      type: 'CenterProps',
      default: '{}'
    },
    {
      name: 'primaryAction',
      type: 'NoVariantButtonProps',
      default: '<Button/>'
    },
    {
      name: 'secondaryActions',
      type: 'NoVariantButtonProps',
      default: 'null'
    },
    {
      name: 'noMessage',
      type: 'boolean',
      default: 'false',
      description: 'When true, no error message is displayed'
    },
    {
      name: 'noActions',
      type: 'boolean',
      default: 'false',
      description: 'When true, no actions are displayed'
    },

]} />

## How to Use

Each predefined error component comes with a default headline, message, and
image, reducing the need for manual setup.

- **NotFoundUI**: Displays "No Data Found" with a default message and a missing
  records image.
- **ForbiddenUI**: Displays "Forbidden Access", indicating a user lacks
  permissions.
- **NotAuthenticatedUI**: Displays "Authentication Required" when a user is not
  logged in.
- **UnknownErrorUI**: Displays a generic error message for unexpected failures.
- **DoneUI**: Used to confirm task completion or indicate that everything is up
  to date.

### Import

```tsx
import {
  ForbiddenUI,
  NotAuthenticatedUI,
  NotFoundUI,
  UnknownErrorUI
  DoneUI
} from '@KyleWiteck/witeck-design/components';
```

### Base variant

```tsx live
<Card>
  <NotFoundUI />
</Card>
```

### Small varinnt

```tsx live
<Card>
  <UnknownErrorUI
    variant="small"
    primaryAction={props => <Button {...props}>Go back</Button>}
  />
</Card>
```
